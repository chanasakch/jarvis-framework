# Review Checklist

<!-- {{name}}-gatekeeper runs this against review-report.md and each review/<reviewer>.md, using tech-spec.md as the upstream artifact. -->

## Blocking
- [ ] REV-01 (auto) every reviewer in `review.reviewers` produced a `review/<reviewer>.md` — Evidence: file listing vs `jarvis.config.yaml review.reviewers`
- [ ] REV-02 (auto) every `review/<reviewer>.md` contains a parseable JSON block — Evidence: fenced JSON block in each reviewer file
- [ ] REV-03 (auto) no open finding at a severity in `review.block_on` from a blocking reviewer — Evidence: review-report.md Findings table `severity` column vs `review.block_on`
- [ ] REV-04 (auto) every finding cites a rule ID and `file:line` — Evidence: Findings/Advisory table `rule`/`file:line` columns
- [ ] REV-05 severities follow conventions.md §4 definitions — Evidence: Findings table `severity` vs the finding's description
- [ ] REV-06 (auto) review-report.md was generated by `jarvis.js merge-review`, not written by hand — Evidence: review-report.md generation comment
- [ ] REV-07 the Verdict matches the Findings table (fail if any blocking-severity finding is open) — Evidence: review-report.md Verdict section vs Findings table
- [ ] REV-08 [ADDED] the diff base recorded in the report matches the branch's merge-base — Evidence: review-report.md diff base note vs `git merge-base`
- [ ] REV-09 [ADDED] (auto) findings resolved in a loop-back are marked resolved with the fixing commit or task — Evidence: Findings table status vs `FIX-<finding-id>` task/commit reference

## Advisory
- [ ] REV-50 the Advisory table lists non-blocking findings separately from Findings — Evidence: review-report.md Advisory table
- [ ] REV-51 [ADDED] the Blocking Set section states the `review.block_on` value used for this run — Evidence: review-report.md Blocking Set section
