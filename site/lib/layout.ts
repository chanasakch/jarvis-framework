/** The one page container: header, footer, docs shell and landing sections all align to it.
 *  Text blocks inside keep their own reading width (max-w-2xl/3xl). */
export const CONTAINER = "mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8";
