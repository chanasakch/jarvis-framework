---
title: Getting Started
description: Install Jarvis, verify the setup, and run your first work item.
---

## Requirements

Two things are always required; the rest only matter if you're using the shipped Go +
React standards.

| Requirement | Profile | Notes |
|---|---|---|
| Node.js ≥18 | Always | Runs `.jarvis/scripts` |
| git | Always | Records `approve`/`force` actors via `user.name` |
| Go | Go + React | `go version` |
| golangci-lint | Go + React | [Install](https://go.dev/dl/) |
| govulncheck | Go + React | `go install golang.org/x/vuln/cmd/govulncheck@latest` |
| Docker | Go + React | Needed for testcontainers integration tests |

`jarvis.js doctor` checks all of these against your actual `PATH` — see [Verify the
setup](#verify-the-setup) below.

## Install

```bash
npx jarvis-framework init --name jarvis
cd .jarvis && npm install
```

`init` never overwrites a file you already own — run it again after upgrading the
framework and your `.jarvis/standards/`, `jarvis.config.yaml` and `CLAUDE.md` are left
untouched. `--name` sets the prefix for every slash command and agent; `--name ops`
would give you `/ops`, `/ops-init` and `ops-architect` instead.

## What gets added to the repo

<FileTree>
<File name="jarvis.config.yaml" />
<File name="CLAUDE.md" />
<Folder name=".claude/">
  <Folder name="agents/">
    <File name="jarvis-po.md" />
    <File name="jarvis-architect.md" />
  </Folder>
  <Folder name="commands/">
    <File name="jarvis.md" />
    <File name="jarvis-init.md" />
  </Folder>
  <File name="settings.json" />
</Folder>
<Folder name=".jarvis/">
  <File name="VERSION" />
  <Folder name="core/">
    <Folder name="rules/" />
    <Folder name="templates/" />
    <Folder name="checklists/" />
    <Folder name="workflows/" />
  </Folder>
  <Folder name="standards/" />
  <Folder name="project/">
    <File name="context.md" />
  </Folder>
  <Folder name="scripts/" />
  <Folder name="state/" />
</Folder>
<Folder name="packages/errors/">
  <File name="registry.yaml" />
</Folder>
<Folder name="docs/">
  <Folder name="work/" />
  <Folder name="adr/" />
  <Folder name="architecture/" />
</Folder>
</FileTree>

`.claude/`, `.jarvis/core/` and `.jarvis/scripts/` are framework-owned — `upgrade` replaces
them. `jarvis.config.yaml`, `.jarvis/standards/`, `.jarvis/project/` and `CLAUDE.md` are
yours; nothing in the framework ever overwrites them.

## Verify the setup

```bash
node .jarvis/scripts/jarvis.js doctor
```

```text
tools:    ✅ node  ✅ git  ❌ go  ❌ golangci-lint  ❌ govulncheck  ❌ docker
paths:    ✅ docs  ✅ adr  · web  · api  ✅ packages  ✅ error_registry  · migrations_mysql  · migrations_mongo  · openapi
registry: ✅ 7 codes
no problems
hint: go → https://go.dev/dl/
hint: golangci-lint → brew install golangci-lint
hint: govulncheck → go install golang.org/x/vuln/cmd/govulncheck@latest
hint: docker → needed for testcontainers integration tests
```

A `·` next to a path means it doesn't exist yet in this repo — that's expected before
`apps/web`/`apps/api` exist. Missing Go tooling only blocks work that actually needs it;
run `/jarvis-init` next to scan the repo and fill in `.jarvis/project/context.md`.

## Your first work item

<Steps>
<Step title="Scan the repo">
```text
/jarvis-init
```
Writes `.jarvis/project/context.md` (stack, repo map, domains, existing patterns,
glossary) from what's actually in your codebase.
</Step>
<Step title="Start a work item">
```text
/jarvis Add OTP login for returning customers
```
The orchestrator classifies the work type, asks up to five intake flag questions in one
message, then runs the phase sequence on its own — see [Workflows](/docs/workflows) for
what each work type actually runs.
</Step>
<Step title="Read the status report">
Every turn ends with one, whatever happened:
```text
── JARVIS ─────────────────────────────
Item:   FEAT-012 OTP login (feature)
Phase:  architecture → passed (attempt 1) ✅
Waiting: human approval
Next:   ! npm run -s jarvis -- approve FEAT-012 architecture
Warnings: none
───────────────────────────────────────
```
</Step>
<Step title="Approve a phase">
`approve` is human-only — Claude is blocked from running it by `guard.js`. Run the exact
command the status report gave you:
```text
! npm run -s jarvis -- approve FEAT-012 architecture
```
The `!` runs it directly in your terminal rather than asking Claude to. See
[Gates](/docs/gates) for what else is human-only and why.
</Step>
</Steps>

## Next

<Callout kind="tip">
`/jarvis-status` shows every active work item and its warnings at any point; `/jarvis-explain <ID>`
explains exactly why one is blocked and what unblocks it.
</Callout>
